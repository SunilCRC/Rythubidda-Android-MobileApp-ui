# React 19 + RN 0.85 ecosystem still has libraries (notably react-native-fast-image)
# whose peer declarations haven't caught up yet. Allow npm to use the legacy
# resolver so plain `npm install` works without remembering the CLI flag.
legacy-peer-deps=true
